{
  "name": "react-to-do-list",
  "version": "1.0.0",
  "description": "Yes, another to-do list to test out a framework",
  "main": "index.js",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "trash dist && parcel build src/index.html --no-source-maps"
  },
  "license": "MIT",
  "dependencies": {
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
